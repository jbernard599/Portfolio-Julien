// window.onload = function() {
    // var itemsToShow = 9; // Nombre d'éléments à afficher initialement
    // var portfolioItems = Array.from(document.querySelectorAll(".portfolio-wrapper .grid-item"));
    // var loadMoreButton = document.querySelector(".load-more a");

    Affiche initialement les éléments désignés
    // portfolioItems.slice(0, itemsToShow).forEach(function(item) {
        // item.classList.remove("hidden");
    // });

    // loadMoreButton.addEventListener("click", function(e) {
        // e.preventDefault();

        Affiche tous les éléments restants lorsque "Voir plus" est cliqué
        // var hiddenItems = Array.from(document.querySelectorAll(".portfolio-wrapper .grid-item.hidden"));
        // hiddenItems.forEach(function(item) {
            // item.classList.remove("hidden");
        // });

        Cachez le bouton "Voir plus" après le clic
        // loadMoreButton.style.display = 'none';
    // });
// }